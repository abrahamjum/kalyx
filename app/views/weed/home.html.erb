
<div class="search-form container-fluid row">
    <%= form_tag(products_path, :method => "get", id: "search-form") do %>

  <div>
<%= text_field_tag :search, params[:search], placeholder: "Search for...", class:"form-control" %>
  </div>
<div>
  <%= submit_tag "Search", :name => nil, class:"btn btn-primary" %>
</div>
    <% end %>

    <!-- <% if @weeds.blank? %>
      <h4>There are no products containing <%= params[:search] %>.</h4>
    <% end %> -->
</div>

<!-- <div class="row">
  <% @weeds.each do |t| %>
    <div class="col-4">
      <%= t.name %>
      <%= t.species %>
      <%= t.price %>
    </div>
  <% end %>
</div> -->
